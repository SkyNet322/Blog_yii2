version: '3'

services:
  db:
    image: postgres
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: blog_test_yii2
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: itsmylife